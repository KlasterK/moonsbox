"use strict";var buffers;function createMaterialCard(materialName,brush){const card=document.createElement("div");card.className="material-palette-card";const img=document.createElement("img");img.className="material-palette-icon",img.loading="lazy",img.src=`assets/materials/${materialName}`,card.appendChild(img);const label=document.createElement("div");return label.className="material-palette-label",label.textContent=materialName,card.appendChild(label),card.addEventListener("click",()=>{for(let el of document.getElementsByClassName("material-palette-card"))el.classList.remove("material-palette-selected");card.classList.add("material-palette-selected"),brush.material=materialName}),card}function initMaterialPalette(brush){const grid=document.getElementById("material-palette-grid");if(grid)if(grid.innerHTML="",buffers&&buffers.material_names&&buffers.material_names.length)for(const name of buffers.material_names){const card=createMaterialCard(name,brush);grid.appendChild(card),brush.material==name&&card.classList.add("material-palette-selected")}else console.warn("Cannot find any materials. Grid will be empty.");else console.error("Cannot find element #material-palette-grid")}